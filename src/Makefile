# Makefile for producer consumer problem

###############################################################
# Variables
GCC     = gcc
OPTIONS = -pthread -lrt -o
###############################################################

###############################################################
###############################################################
# Rules
###############################################################
###############################################################

###############################################################
# Compile producer and consumer
problem:
	$(GCC) producer.cpp $(OPTIONS) producer
	$(GCC) consumer.cpp $(OPTIONS) consumer

###############################################################

###############################################################
# Make the problem run
run:
	./producer & ./consumer &

###############################################################
# Remove executables
clean:
	rm -f producer
	rm -f consumer

###############################################################
